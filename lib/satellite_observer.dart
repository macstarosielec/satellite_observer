/// Pure-Dart engine for SGP4 satellite propagation, topocentric look-angles,
/// pass prediction, and naked-eye visibility.
///
/// This is the public barrel - the only entry point consumers import. The
/// public API lands incrementally as the L1-L4 capability layers are built
/// (see the implementation plan); this scaffold release exports nothing yet.
library;
