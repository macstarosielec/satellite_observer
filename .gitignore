# Dart / pub
.dart_tool/
.packages
build/
coverage/
doc/api/

# A library should not commit its lockfile.
pubspec.lock

# OS / editor
.DS_Store
.idea/
*.iml
.vscode/

# npm artefacts (not relevant to a Dart package)
package-lock.json

# Fixture-generation tooling artefacts (reference only, NEVER bundled - the
# package stays analytic / ephemeris-free per ADR-2 / NFR-5).
.venv/
*.bsp
